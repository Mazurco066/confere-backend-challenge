const receivementRepository = require('./receivementRepository')
const transactionRepository = require('./transactionRepository')

module.exports = {
  receivementRepository,
  transactionRepository
}